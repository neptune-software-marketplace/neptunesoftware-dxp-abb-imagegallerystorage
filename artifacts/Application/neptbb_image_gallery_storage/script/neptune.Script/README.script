// Fetch all of your images from a database, view them then, upload any extra images or delete a picture that you wish. The API's do all the updates.
// You may also upload pictures (or take pictures if you are using a mobile phone) into the carousel then, call the API to save the images into your corresponding database. 
// Don't forger to onnect the neptune.restapi's with your corresponding API's.